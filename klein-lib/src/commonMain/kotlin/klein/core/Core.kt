package klein.core

/**
 * The Klein Core IR.
 *
 * This package will hold the Core intermediate representation and the lowering
 * from the surface syntax (see [klein.surface]) into it. It is intentionally
 * empty for now, reserving a clean namespace for Core nodes (App, Lam, If, ...)
 * that would otherwise clash with the surface AST (Apply, Lambda, IfThenElse).
 */
